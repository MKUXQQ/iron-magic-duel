# 法术决斗 MOD 指令说明

本文档对应当前已部署版本：

- Minecraft 1.20.1 Forge：`iron_magic_duel 1.0.64`
- Minecraft 1.21.1 NeoForge：`iron_magic_duel 1.0.54`

`duel_N` 表示决斗组 ID，例如 `duel_1`、`duel_2`。输入组 ID 的指令支持 Tab 自动补全。

权限说明：标有“管理员”的指令需要权限等级 2（OP）；标有“玩家”的指令普通玩家可以使用。

## 决斗开始与停止

| 指令 | 权限 | 功能 |
|---|---:|---|
| `/spell_duel start all` | 管理员 | 尝试开始全部已经配置完成的决斗组。缺少玩家或 A/B 点位的组不会开始。 |
| `/spell_duel start duel_N` | 管理员 | 开始指定决斗组。要求 A、B 队均有玩家，并且已设置 A、B 点位。 |
| `/spell_duel stop all` | 管理员 | 立即停止全部正在进行的决斗，恢复参与玩家和观战玩家；保留 A/B 点位。 |
| `/spell_duel stop duel_N` | 管理员 | 立即停止指定的活动决斗，恢复参与玩家和观战玩家；保留该组 A/B 点位。 |

停止决斗不会删除决斗组的点位配置。停止后可以重新打开玩家选择器、选择新玩家并再次使用同一个决斗组。

## 工具与商店

| 指令 | 权限 | 功能 |
|---|---:|---|
| `/spell_duel tool` | 管理员 | 获得玩家选择器和点位工具。 |
| `/spell_duel shop` | 管理员 | 获得商店木桶和商店编辑器。 |

### 玩家选择器操作

- 手持玩家选择器右键：打开全部在线玩家列表。
- 左键点击玩家：加入当前决斗组 A 队。
- 右键点击玩家：加入当前决斗组 B 队。
- 点击当前编辑组中已经选择的玩家：取消该玩家的选择。
- 按 `Esc`：取消本次编辑。
- 点击底部“创建对战”：保存当前选择。即使当前人数不足，也允许创建；真正开始决斗时才检查 A/B 队人数和点位。
- 玩家名单来自服务端全部在线玩家，按照 UUID 去重，不受视距和客户端实体加载范围影响。
- 如果目标玩家属于其他未开始的决斗组，会自动转入当前组。
- 如果目标玩家正在其他活动决斗中，会先安全停止原决斗并恢复参与者，再把目标玩家加入当前组。

### 点位工具操作

- 右键方块：设置临时 A 点。
- 左键方块：设置临时 B 点。
- 蹲下右键：把临时 A/B 点绑定为新的决斗组点位。
- 主手或副手持有点位工具时：显示已保存点位。
  - A 点使用白色粒子。
  - B 点使用黑色粒子。
  - 粒子上方显示 `duel_N · A` 或 `duel_N · B`。
  - 文字和粒子只对当前持有点位工具的玩家可见；放下或切换工具后立即隐藏。

## 观战与 HUD

| 指令 | 权限 | 功能 |
|---|---:|---|
| `/spell_duel spectate duel_N` | 玩家 | 进入指定的、正在进行中的决斗组观战。 |
| `/spell_duel spectate leave` | 玩家 | 退出观战并恢复观战前的位置和游戏模式。 |
| `/spell_duel hud <x> <y>` | 玩家 | 设置当前玩家观战 HUD 的屏幕坐标；`x`、`y` 范围为 `0–10000`。 |

观战 HUD 会显示对战玩家的名称、头像、生命值、蓝量、Curios 法术书中的法术和冷却。普通非观战玩家不会显示其他玩家的决斗 HUD。

## 清理指令

| 指令 | 权限 | 功能 |
|---|---:|---|
| `/spell_duel clear all` | 管理员 | 删除全部决斗组、玩家分组和点位配置。之后新组重新从 `duel_1` 开始。 |
| `/spell_duel clear group` | 管理员 | 清空所有决斗组中的玩家，但保留所有 A/B 点位。 |
| `/spell_duel clear group duel_N` | 管理员 | 清空指定决斗组中的玩家，但保留该组 A/B 点位。 |
| `/spell_duel clear point` | 管理员 | 清除全部决斗组的 A/B 点位。 |
| `/spell_duel clear point duel_N` | 管理员 | 清除指定决斗组的 A/B 点位。 |

注意：`clear all` 会删除点位配置；如果只想结束当前战斗并保留点位，请使用 `/spell_duel stop all` 或 `/spell_duel stop duel_N`。

## 假玩家

| 指令 | 权限 | 功能 |
|---|---:|---|
| `/spell_duel fake_players` | 管理员 | 按服务器配置中的名字执行假玩家生成命令。聊天提示只显示“召唤假人”。 |

该功能依赖服务器存在兼容的 `/player <名字> spawn` 命令。假玩家名称可以在 MOD 的服务器配置中修改。

## 法术弹道重力

以下两组指令功能相同，均需要管理员权限：

| 指令 | 功能 |
|---|---|
| `/ironspell gravity on` | 关闭铁魔法法术投射物的重力，使当前及之后生成的法术投射物不下坠。 |
| `/ironspell gravity off` | 恢复铁魔法法术投射物的重力。 |
| `/spellgravity on` | `/ironspell gravity on` 的简写。 |
| `/spellgravity off` | `/ironspell gravity off` 的简写。 |

## 仅 NeoForge 1.21.1 存在的指令

以下旧兼容指令当前只在 NeoForge 1.21.1 版本注册，Forge 1.20.1 中没有：

| 指令 | 权限 | 功能 |
|---|---:|---|
| `/spell_duel point duel_N a` | 管理员 | 把执行者当前位置设置为指定决斗组的 A 点。 |
| `/spell_duel point duel_N b` | 管理员 | 把执行者当前位置设置为指定决斗组的 B 点。 |
| `/spell_duel display on` | 管理员 | 开启旧版全局 HUD 显示开关。 |
| `/spell_duel display off` | 管理员 | 关闭旧版全局 HUD 显示开关。 |

Forge 1.20.1 已按之前的要求删除 `/spell_duel point` 和 `/spell_duel display` 根指令；Forge 端请使用点位工具设置点位。

## 常用流程示例

1. 管理员输入 `/spell_duel tool` 获得工具。
2. 使用点位工具设置 A、B 点，然后蹲下右键创建点位组。
3. 使用玩家选择器打开全部在线玩家列表，左键选择 A 队、右键选择 B 队。
4. 点击“创建对战”保存选择。
5. 输入 `/spell_duel start duel_1` 开始决斗。
6. 需要强制结束时输入 `/spell_duel stop duel_1`。
7. 需要清空玩家并保留点位时输入 `/spell_duel clear group duel_1`。

